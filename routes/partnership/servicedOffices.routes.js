module.exports = (app) => {
    const chcekAuth = require('../../middlewares/partnership/auth');
    // const occupancy = require('../../controllers/partnership/occupancy');

    // app.get('/partnership/serviced-offices', chcekAuth, occupancy.getSo);
    // app.get('/partnership/serviced-offices/:locationId', chcekAuth, occupancy.getSo);
}